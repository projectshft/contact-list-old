import React from 'react';

const Header = () => (
  <div>
    <span>
      <h1>Contacts</h1>
      <button id="add-contact-button" class="btn btn-primary" onClick={this.handleClick} >Add Contact</button>
    </span>
  </div>
)

export default Header
